defmodule Adapters.Dms do
  @moduledoc """
  Context module for Dms.  This module acts as a "gateway" module defining
  the "boundary" for Dms database access.  Consider Adapters.Dms.* modules
  to be "private modules".  If in the future we would like to enforce these
  boundary conditions at compile time, consider using Sasa Juric's Boundary
  library:

  https://hex.pm/packages/boundary

  """
  # ACCESS FUNCTIONS

  # MUTATION FUNCTIONS
end
